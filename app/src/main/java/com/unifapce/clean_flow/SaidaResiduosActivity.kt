package com.unifapce.clean_flow

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class SaidaResiduosActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Defina um layout para esta Activity (ex: R.layout.activity_saida_residuos)
        // setContentView(R.layout.activity_saida_residuos)
        // Por enquanto, você pode simplesmente mostrar uma mensagem
        // Toast.makeText(this, "Tela de Saída de Resíduos", Toast.LENGTH_SHORT).show()
    }
}