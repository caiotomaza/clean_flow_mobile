package com.unifapce.clean_flow

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class ConsultaResiduosActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Defina um layout para esta Activity (ex: R.layout.activity_consulta_residuos)
        // setContentView(R.layout.activity_consulta_residuos)
        // Toast.makeText(this, "Tela de Consulta de Resíduos", Toast.LENGTH_SHORT).show()
    }
}